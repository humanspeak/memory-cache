export const load = () => ({
    title: 'Multi-Tenant Caching | Examples | Memory Cache',
    description:
        'Implement per-tenant cache isolation with namespaced keys in @humanspeak/memory-cache, using prefix and wildcard deletion for scoped cleanup.'
})
