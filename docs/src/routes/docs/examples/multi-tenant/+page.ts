export const load = () => ({
    title: 'Multi-Tenant Caching | Examples | Memory Cache',
    description:
        'Implement isolated per-tenant caches with namespace support in Memory Cache. Keep tenant data separated with independent TTL, eviction policies, and cache statistics per namespace.'
})
