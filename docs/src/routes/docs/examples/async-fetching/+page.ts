export const load = () => ({
    title: 'Async Fetching | Examples | Memory Cache',
    description:
        'Cache async fetch results with automatic request deduplication and stale-while-revalidate patterns using Memory Cache. Prevent redundant network calls in your TypeScript backend.'
})
