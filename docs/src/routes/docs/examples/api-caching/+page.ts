export const load = () => ({
    title: 'API Caching | Examples | Memory Cache',
    description:
        'Cache API responses with @humanspeak/memory-cache to reduce latency and external calls using TTL expiration and invalidation patterns in apps.'
})
