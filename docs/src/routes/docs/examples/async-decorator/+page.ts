export const load = () => ({
    title: 'Async Decorator | Examples | Memory Cache',
    description:
        'Use the @cached decorator with async methods for single-flight request deduplication, retry-safe errors, TTL caching, and nullish results.'
})
