export const load = () => ({
    title: 'Async Decorator | Examples | Memory Cache',
    description:
        'Use @cached with async methods for single-flight request deduplication, retry-safe errors, TTL caching, and nullish result handling in apps.'
})
