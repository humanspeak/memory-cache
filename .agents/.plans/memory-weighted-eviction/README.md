# Memory-weighted eviction implementation plans

Generated by the improve skill on 2026-08-27. Execute the plan below as one
coherent feature: runtime semantics first, then documentation and the live
website example. The executor must read the plan fully, honor its STOP
conditions, and update the status row when done.

## Execution order and status

| Plan | Title                                                      | Priority | Effort | Depends on | Status |
| ---- | ---------------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Add weight-bounded LRU eviction and document it end to end | P1       | M      | —          | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- There is one plan. Its internal order is significant: red tests establish the
  contract before implementation, and the website demo depends on the new
  runtime API.

## Finding summary

| Finding                          | Category                       | Impact                                                                            | Effort | Risk                                         | Confidence | Evidence                                                                                                                                    |
| -------------------------------- | ------------------------------ | --------------------------------------------------------------------------------- | ------ | -------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Add memory-weighted LRU eviction | Direction / performance safety | Entry-count limits do not bound memory-like cost for variably sized API responses | M      | MED — accounting touches every mutation path | HIGH       | `src/cache.ts:532-585`, `README.md:21`, `docs/src/routes/docs/examples/api-caching/+page.svx:34-55`, `docs/src/lib/compare-data.ts:160-163` |

## Dependency graph

```text
failing runtime tests
        ↓
weight accounting + eviction semantics
        ↓
existing stats/decorator integration tests
        ↓
README + API/use-case/comparison docs
        ↓
interactive website example + full verification
```

## Findings considered and rejected

- Reinterpret `maxSize` as a byte/weight limit: rejected because it would break
  the existing documented entry-count API.
- Automatically estimate JavaScript object memory: rejected because retained
  heap size cannot be measured accurately and portably in a zero-dependency
  browser-and-Node library.
- Add stale-while-revalidate in the same change: rejected as a separate open
  feature with distinct API, concurrency, and documentation concerns.
- Add `maxEntryWeight` and eviction-reason hook fields now: deferred to avoid
  expanding the first weighted-eviction release and changing hook contracts.
